export interface ICreateAnimalsRequest {
  id: string;
  name: string;
  birthDate: string;
  breed: string;
  species: string;
  user_id: string;
}

export interface IEspecies {
  id: string;
  name: string;
}
